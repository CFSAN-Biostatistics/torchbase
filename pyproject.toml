[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
    "miniwdl: tests requiring miniwdl and Docker (skipped unless CI=true)",
]
addopts = "-m 'not miniwdl' --tb=short"